diff --git a/RecipeApp.tsx b/RecipeApp.tsx
index 8bfaacf..a062400 100644
--- a/RecipeApp.tsx
+++ b/RecipeApp.tsx
@@ -41,11 +41,18 @@ import {
 } from './types';
 import { apiFetch, readApiError } from './lib/api';
 import { preloadImages, waitForPreloadedImages } from './lib/client/image-cache';
+import {
+  TIMER_STORAGE_KEY,
+  LEGACY_TIMER_STORAGE_KEY,
+  readLocalStorage,
+  loadStoredTimers,
+  mergeCategories,
+  dataURItoBlob,
+} from './lib/client/recipe-app-state';
+import { buildRecipeUpdatePayload } from './lib/domain/recipe-update-payload';
 
 const INITIAL_RECIPES: Recipe[] = [];
 const INITIAL_USER_CATEGORIES: CategoryItem[] = DEFAULT_USER_CATEGORY_ITEMS.map((category) => ({ ...category }));
-const TIMER_STORAGE_KEY = 'feji_active_timers:v2';
-const LEGACY_TIMER_STORAGE_KEY = 'feji_active_timers';
 const CHATGPT_SIGN_OUT_PATH = '/signout-with-chatgpt?return_to=%2F';
@@ -63,66 +70,6 @@ type RecipeAppProps = {
   initialJoinToken?: string | null;
 };
 
-function readLocalStorage(key: string) {
-  return typeof window === 'undefined' ? null : window.localStorage.getItem(key);
-}
-
-function loadStoredTimers(): ActiveTimer[] {
-  const saved = readLocalStorage(TIMER_STORAGE_KEY) ?? readLocalStorage(LEGACY_TIMER_STORAGE_KEY);
-  if (!saved) return [];
-  try {
-    return (JSON.parse(saved) as ActiveTimer[]).map((timer) => {
-      if (!timer.isRunning || !timer.endTime) return timer;
-      return {
-        ...timer,
-        remainingSec: Math.max(0, Math.ceil((timer.endTime - Date.now()) / 1000)),
-      };
-    });
-  } catch {
-    return [];
-  }
-}
-
-function mergeCategories(baseCategories: CategoryItem[], extraCategories: CategoryItem[]) {
-  const mergedCategories = [...baseCategories];
-  const seenNames = new Set(baseCategories.map((category) => category.name.trim().toLowerCase()));
-
-  extraCategories.forEach((category) => {
-    const trimmedName = category.name.trim();
-    if (!trimmedName) return;
-    const normalizedName = trimmedName.toLowerCase();
-    if (seenNames.has(normalizedName)) return;
-    seenNames.add(normalizedName);
-    mergedCategories.push({ id: category.id || trimmedName, name: trimmedName, iconName: category.iconName, isCustom: category.isCustom ?? true });
-  });
-
-  return mergedCategories;
-}
-
-function dataURItoBlob(dataURI: string) {
-  const byteString = atob(dataURI.split(',')[1]);
-  const mimeString = dataURI.split(',')[0].split(':')[1].split(';')[0];
-  const ab = new ArrayBuffer(byteString.length);
-  const ia = new Uint8Array(ab);
-  for (let i = 0; i < byteString.length; i++) ia[i] = byteString.charCodeAt(i);
-  return new Blob([ab], { type: mimeString });
-}
-
 type ViewState = 'HOME' | 'DETAIL' | 'COOKING' | 'ADD_RECIPE' | 'EDIT_RECIPE';
@@ -1584,7 +1531,7 @@ const App: React.FC<RecipeAppProps> = ({ email, initialDisplayName, initialJoinT
                 await apiFetch(`/api/recipes/${recipe.id}`, accessToken, {
                   method: 'PUT',
                   headers: { 'Content-Type': 'application/json' },
-                  body: JSON.stringify({ name: recipe.name, time: recipe.time, category: newName, servings: recipe.servings, ingredients: recipe.ingredients, instructions: recipe.instructions, stepIngredients: recipe.stepIngredients || {}, removeImage: false }),
+                  body: JSON.stringify(buildRecipeUpdatePayload(recipe, newName)),
                 });
@@ -1616,7 +1563,7 @@ const App: React.FC<RecipeAppProps> = ({ email, initialDisplayName, initialJoinT
                   await apiFetch(`/api/recipes/${recipe.id}`, accessToken, {
                     method: 'PUT',
                     headers: { 'Content-Type': 'application/json' },
-                    body: JSON.stringify({ name: recipe.name, time: recipe.time, category: targetCategory.name, servings: recipe.servings, ingredients: recipe.ingredients, instructions: recipe.instructions, stepIngredients: recipe.stepIngredients || {}, removeImage: false }),
+                    body: JSON.stringify(buildRecipeUpdatePayload(recipe, targetCategory.name)),
                   });
diff --git a/components/ShoppingListModal.tsx b/components/ShoppingListModal.tsx
index c88eee7..27ceba6 100644
--- a/components/ShoppingListModal.tsx
+++ b/components/ShoppingListModal.tsx
@@ -2,6 +2,7 @@ import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';
 import { ArrowLeft, Check, ChevronDown, ChevronRight, FolderPlus, Loader2, Minus, Pencil, Plus, Search, ShoppingCart, SlidersHorizontal, Trash2 } from 'lucide-react';
 import { apiFetch } from '../lib/api';
 import { CookbookMember, Recipe, ShoppingCategory, ShoppingListItem } from '../types';
+import { formatQuantity, normalizeIngredientName, parseIngredientLine } from '../lib/domain/ingredient-parser';
@@ -12,70 +13,6 @@ interface ShoppingListModalProps {
   onClose: () => void;
 }
-
-// Ingredient parser implementation moved byte-for-byte in behavior to lib/domain/ingredient-parser.ts.
-// No rendering, network, polling, optimistic update or import sequencing changed.
 
 // ─── Component ────────────────────────────────────────────────────────────────
